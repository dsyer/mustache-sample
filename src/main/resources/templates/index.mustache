{{<layout}}{{$menu}}home{{/menu}}{{$body}}
	<h1>Demo</h1>
	<div class="container">
		<div class="col-sm-offset-2 col-sm-10">Hello World</div>
		{{#form}}<form method="post">
			<div class="form-group has-feedback">
				{{#field}}{{#render}}{{/render}}{{/field}}
				{{#date}}{{#render}}{{/render}}{{/date}}
				<input type="hidden" name="_csrf" value="{{_csrf.token}}" />
			</div>
			<div class="form-group">
				<div class="col-sm-offset-2 col-sm-10">
					<button class="btn btn-primary" type="submit">Submit</button>
				</div>
			</div>
		</form>{{/form}}
	</div>
	{{#foo}}<div class="container">
		<div class="col-sm-offset-2 col-sm-10">Value is: {{value}}</div>
		<div class="col-sm-offset-2 col-sm-10">Date is: {{date}}</div>
	</div>{{/foo}}
{{/body}}{{/layout}}